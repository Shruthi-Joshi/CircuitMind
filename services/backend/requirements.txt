fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg[binary]==3.2.3
pgvector==0.3.3
redis==5.0.8
python-multipart==0.0.12
sse-starlette==2.1.3

# ── Multi-agent engine (LangGraph human-in-the-loop) ──────────────────────────
# 0.2.28 did NOT expose `interrupt()` / `Command` in `langgraph.types`, which
# broke every code path that imports the graph or the worker. 1.2.8 provides
# both primitives. The companion packages below are the LangGraph runtime
# dependencies and are pinned to versions compatible with langgraph 1.2.8
# (langchain-core<2,>=1.4.7; langgraph-checkpoint<5,>=4.1.0;
#  langgraph-prebuilt<1.2,>=1.1.0; langgraph-sdk<0.5,>=0.4.2).
langgraph==1.2.8
langgraph-checkpoint==4.2.0
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.4
langchain-core==1.6.1

numpy==1.26.4
openpyxl==3.1.5
pypdf==5.0.1
pillow==10.4.0
pytesseract==0.3.13
sentence-transformers==3.1.1
